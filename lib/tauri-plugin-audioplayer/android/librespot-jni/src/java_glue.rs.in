//Automatically generated by rifgen
use crate::*;
use jni_sys::*;
foreign_callback!(
	callback LibrespotCallbacks {
		self_type LibrespotCallbacks;
		onPlay = LibrespotCallbacks::on_play(& self);
		onStop = LibrespotCallbacks::on_stop(& self);
		onPause = LibrespotCallbacks::on_pause(& self);
		onTimeChange = LibrespotCallbacks::on_time_change(& self , pos : u64);
		onEnded = LibrespotCallbacks::on_ended(& self);
		onSeek = LibrespotCallbacks::on_seek(& self , pos : u64);
		onConnected = LibrespotCallbacks::on_connected(& self);
	}
);
foreign_class!(
	class LibrespotWrapper {
		self_type LibrespotWrapper;
		constructor LibrespotWrapper::new(callbacks : Box < dyn LibrespotCallbacks >)->LibrespotWrapper;
		fn LibrespotWrapper::load(& self , src : String , autoplay : bool); alias load;
		fn LibrespotWrapper::play(& self); alias play;
		fn LibrespotWrapper::pause(& self); alias pause;
		fn LibrespotWrapper::initialize_librespot(& self , credentials_path : String , audio_path : String , access_token : String ,); alias initializeLibrespot;
		fn LibrespotWrapper::initialize_android_context(); alias initializeAndroidContext;
	}
);
