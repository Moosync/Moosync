load("@rules_rust//rust:defs.bzl", "rust_library")

rust_library(
    name = "mod",
    srcs = ["mod.rs"],
    deps = [
        "//core/types_ui",
        "//ui/src/utils:extensions",
    ],
)

rust_library(
    name = "request_handler",
    srcs = ["request_handler.rs"],
    deps = [
        "//core/types_ui",
        "//ui/src/utils:extensions",
    ],
)
