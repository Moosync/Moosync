import { ArtistModel } from "../types/song_view.slint";
import { GridListView } from "../common/grid_list_view.slint";
import { Constants, Theme, Pages, SelectedEntity } from "../constants.slint";

export component ArtistsPage inherits Rectangle {
    in property <[ArtistModel]> artists: Constants.testArtists;
    in-out property <Pages> active-page;
    in-out property <SelectedEntity> selected-entity;

    background: transparent;

    VerticalLayout {
        spacing: Theme.spacingXxl;
        alignment: start;

        text := Text {
            height: Theme.pageHeader + Theme.paddingLg;
            text: "Artists";
            font-size: Theme.pageHeader;
            color: Theme.textPrimary;
        }

        GridListView {
            height: root.height - text.height - (Theme.spacingXxl + Theme.paddingLg);
            item-width: Theme.cardWidth;
            total-items: root.artists.length;
            item-title(idx) => {
                root.artists[idx].title
            }
            item-cover(idx) => {
                root.artists[idx].coverPath
            }
            item-clicked(idx) => {
                root.selected-entity = {
                    id: root.artists[idx].id,
                    title: root.artists[idx].title,
                    cover-path: root.artists[idx].coverPath,
                };
                root.active-page = Pages.ArtistContent;
            }
        }
    }
}
