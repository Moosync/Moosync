import { PlaylistModel } from "../types/song_view.slint";
import { GridListView } from "../common/grid_list_view.slint";
import { Constants, Theme, Pages, SelectedEntity } from "../constants.slint";

export component PlaylistsPage inherits Rectangle {
    in property <[PlaylistModel]> playlists: Constants.testPlaylists;
    in-out property <Pages> active-page;
    in-out property <SelectedEntity> selected-entity;

    background: transparent;

    VerticalLayout {
        spacing: Theme.spacingXxl;
        alignment: start;

        text := Text {
            height: Theme.pageHeader + Theme.paddingLg;
            text: "Playlists";
            font-size: Theme.pageHeader;
            color: Theme.textPrimary;
        }

        GridListView {
            height: root.height - text.height - (Theme.spacingXxl + Theme.paddingLg);
            item-width: Theme.cardWidth;
            total-items: root.playlists.length;
            item-title(idx) => {
                root.playlists[idx].title
            }
            item-cover(idx) => {
                root.playlists[idx].coverPath
            }
            item-clicked(idx) => {
                root.selected-entity = {
                    id: root.playlists[idx].id,
                    title: root.playlists[idx].title,
                    cover-path: root.playlists[idx].coverPath,
                };
                root.active-page = Pages.PlaylistContent;
            }
        }
    }
}
