preferences:
  - id: ExtensionRegistries
    component: TextArrayInput
    title: Extension Registries
    subtitle: Add or remove extension registry URLs (manifest.json)
    placeholder: https://example.com/manifest.json
