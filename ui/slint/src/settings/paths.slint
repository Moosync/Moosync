import { AppPreferences } from "preferences.slint";
import { PreferenceList } from "preference_list.slint";
import { SettingsPageLayout } from "settings_page_layout.slint";

export component PathsPage inherits SettingsPageLayout {
    title: @tr("Settings > Paths");

    PreferenceList {
        items: AppPreferences.paths_items;
    }
}
