export struct ExtensionItem {
    name: string,
    package_name: string,
    version: string,
    active: bool,
    is_installed: bool,
    loading: bool,
    description: string,
    icon: image,
    has_started: bool,
    icon_url: string,
    registry: string,
}
